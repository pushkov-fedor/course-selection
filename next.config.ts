import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export", // Отключено для dev режима с API
  // images: {
  //   unoptimized: true,
  // },
  // basePath: "/course-selection", // Только для GitHub Pages
  // assetPrefix: "/course-selection",
};

export default nextConfig;
